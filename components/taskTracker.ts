import react from "react";

export default function TaskTracker(){
    class Timer {
        constructor(start : String) {
            const speed = start    
        }

        speed: int = this.speed
    }
   const starter  = new Timer("hello");


    let go = starter.speed
    return go
}